<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des étiquettes</title>
    <link rel="stylesheet" href="{{ asset('css/bootstrap-4/css/bootstrap.min.css') }}">
    <style>
        .img {
            width: 50px;
            height: auto;
            transition: transform 0.2s ease-in-out;
        }
        .img:hover {
            transform: scale(1.1);
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Liste des étiquettes</h1>
        <form method="get" action="{{ path('etiquette_search') }}" class="mb-4">
            <div class="form-row">
                <div class="col">
                    <input type="text" name="etat" class="form-control" placeholder="Rechercher une étiquette par État">
                </div>
                <div class="col">
                    <button type="submit" class="btn btn-primary">Rechercher</button>
                </div>

                {# Cette div va m'afficher l'image d'ajout d'étiquette Fodé Kerfala Camara 17/06/2024#}
                <div class="col text-right">
                    <a href="{{ path('ajouter') }}">
                        <img class="img" src="{{ asset('images/ajouterEtiquette.png') }}" alt="Ajouter une étiquette">
                    </a>
                </div>
            </div>
        </form>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>N°</th>
                    <th>État</th>
                    <th>Disponibilité</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for etiquette in etiquettes %}
                    <tr>
                        <td>{{ etiquette.id }}</td>
                        <td>{{ etiquette.etat }}</td>
                        <td>{{ etiquette.disponibilite ? 'Disponible' : 'Indisponible' }}</td>
                        <td>
                            <a href="{{ path('etiquette_edit', {'id': etiquette.id}) }}" class="btn btn-warning btn-sm">Modifier</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>
</html>