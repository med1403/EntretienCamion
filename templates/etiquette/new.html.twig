<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Gestion de Camion | Ajout d'une Etiquette</title>
    <link rel="stylesheet" href="{{ asset('css/bootstrap-4/css/bootstrap.min.css') }}">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Ajout d'une Etiquette</h1>
        
        {{ form_start(form) }}
        
        <div class="form-group">
            {{ form_row(form.etat, {
                'attr': {
                    'class': 'form-control'
                },
                'label_attr': {
                    'class': 'form-label'
                }
            }) }}
        </div>
        
        <div class="form-group">
            {{ form_row(form.disponibilite, {
                'attr': {
                    'class': 'form-control'
                },
                'label_attr': {
                    'class': 'form-label'
                }
            }) }}
        </div>

        <div class="form-group">
            {{ form_row(form.save, {
                'attr': {
                    'class': 'btn btn-primary'
                }
            }) }}
        </div>

        {{ form_end(form) }}
                
    </div>
</body>
</html>
