{% extends 'base.html.twig' %}

{% block body %}
    <div class="card mt-5 shadow-sm">
        <div class="card-header">
            <h3 class="mb-0"><i class="fas fa-plus-circle"></i> Nouvelle Réparation</h3>
        </div>
        <div class="card-body">
            {{ form_start(form) }}
            <div class="form-group">
                {{ form_label(form.incident) }}
                {{ form_widget(form.incident, {'attr': {'class': 'form-control', 'placeholder': 'Enter incident ID'}}) }}
            </div>
            <div class="form-group">
                {{ form_label(form.date_reparation) }}
                {{ form_widget(form.date_reparation, {'attr': {'class': 'form-control', 'placeholder': 'Enter repair date'}}) }}
            </div>
            <div class="form-group">
                {{ form_label(form.description) }}
                {{ form_widget(form.description, {'attr': {'class': 'form-control', 'placeholder': 'Enter description'}}) }}
            </div>
            <div class="form-group">
                {{ form_label(form.cout) }}
                {{ form_widget(form.cout, {'attr': {'class': 'form-control', 'placeholder': 'Enter cost'}}) }}
            </div>
            <button class="btn btn-primary"><i class="fas fa-save"></i> Enregistrer</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
