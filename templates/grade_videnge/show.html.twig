{% extends 'admin/base.html.twig' %}
{% block contenu %}

{% block title %}GradeVidenge{% endblock %}


    <h1>GradeVidenge</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grade_videnge.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ grade_videnge.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_grade_videnge_index') }}">back to list</a>

    <a href="{{ path('app_grade_videnge_edit', {'id': grade_videnge.id}) }}">edit</a>

    {{ include('grade_videnge/_delete_form.html.twig') }}
{% endblock %}
