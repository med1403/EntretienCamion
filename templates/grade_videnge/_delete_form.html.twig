<form method="post" action="{{ path('app_grade_videnge_delete', {'id': grade_videnge.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ grade_videnge.id) }}">
    <button class="btn">Delete</button>
</form>
