{% extends 'admin/base.html.twig' %}
{% block title %}Tracteur{% endblock %}
{% block contenu %}

    <h1>Tracteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tracteur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ tracteur.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tracteur_index') }}">back to list</a>

    <a href="{{ path('app_tracteur_edit', {'id': tracteur.id}) }}">edit</a>

    {{ include('tracteur/_delete_form.html.twig') }}
{% endblock %}
