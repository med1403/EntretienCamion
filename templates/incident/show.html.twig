{% extends 'base.html.twig' %}

{% block body %}
    <div class="card mt-5 shadow-sm">
        <div class="card-header">
            <h3 class="mb-0">Incidence Details</h3>
        </div>
        <div class="card-body">
            <p><strong>Type:</strong> {{ incidence.type }}</p>
            <p><strong>Description:</strong> {{ incidence.description }}</p>
            <a href="{{ path('incident_list') }}" class="btn btn-secondary">Révenir à la Liste</a>
        </div>
    </div>
{% endblock %}
