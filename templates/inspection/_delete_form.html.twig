<form method="post" action="{{ path('app_inspection_delete', {'id': inspection.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inspection.id) }}">
    <button class="btn">Delete</button>
</form>
